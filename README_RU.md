# SplitText.jsx - Документация

[🇷🇺 Русская версия](README_RU.md) | [🇬🇧 English Version](README.md)

## 📖 Описание

**SplitText.jsx** - это скрипт для Adobe After Effects, предназначенный для интеллектуального разделения текстовых слоев с учетом правил форматирования, географических названий, названий компаний и других специальных терминов.

**Версия**: 1.2  
**Автор**: tannenspiel@gmail.com  
**Язык**: ExtendScript (JavaScript для Adobe)

## 🎯 Основные возможности

### 1. Разделение текста на слои
- Разделяет текстовый слой на два отдельных слоя
- Поддерживает множественный выбор слоев
- Автоматически копирует свойства исходного слоя (позиция, масштаб, поворот, прозрачность)
- Поддерживает разделение по символу или автоматически по середине

### 2. Разделение текста на строки
- Разделяет длинную строку на две строки в том же слое
- Рекурсивно обрабатывает все строки в тексте
- Поддерживает разные типы переносов строк (\r\n, \r, \n)

### 3. Переименование и нумерация слоев
- Переименовывает и нумерует выбранные слои (текстовые и не-текстовые)
- Сортирует слои по времени начала (inPoint)
- Поддерживает маски имен с плейсхолдером `{num}`
- 4-значная нумерация (0001, 0002, 0003...)
- **Чекбокс "Up"**: Инвертирует порядок нумерации и сортировки
  - Когда выключен: самый ранний inPoint → номер 0001 (вверху)
  - Когда включен: самый ранний inPoint → самый большой номер (внизу)

## 🚀 Использование

### Установка
1. Скопируйте файл `SplitText.jsx` в папку скриптов After Effects:
   - **Windows**: `C:\Program Files\Adobe\Adobe After Effects [версия]\Support Files\Scripts\`
   - **Mac**: `/Applications/Adobe After Effects [версия]/Scripts/`

2. Запустите скрипт через меню After Effects:
   - **File** → **Scripts** → **Run Script File...**
   - Или используйте панель **Window** → **Scripts**

### Интерфейс

#### Панель "Actions"
- **Split into Layers** - разделяет текст на два отдельных слоя
- **Split into Lines** - разделяет текст на две строки в том же слое

#### Панель "Options"
- **Use Split Symbol** - использовать символ разделения (указывается в поле ниже)
- **Split at Layer Middle** - автоматически разделять по середине времени слоя
- **Split Symbol** - поле для ввода символа разделения (по умолчанию: `$`)

#### Панель "Rename and Number Layers"
- **Name Mask** - маска для имени слоя (можно использовать `{num}` для номера)
- **Rename & Number Layers** - кнопка для переименования и нумерации
- **Up** - чекбокс для инвертирования порядка нумерации и сортировки
  - **Выключен (по умолчанию)**: Самый ранний inPoint получает номер 0001 и размещается вверху
  - **Включен**: Самый ранний inPoint получает самый большой номер и размещается внизу

## 📋 Правила форматирования

Скрипт использует интеллектуальные правила для предотвращения некорректного разделения текста:

### 1. Слова, которые не должны быть в конце строки
- **ТОЛЬКО**: предлоги, артикли, союзы, местоимения (русские и английские)
- **НЕ включает**: вспомогательные глаголы, наречия, количественные слова (убраны в v1.2 для устранения конфликтов)

### 2. Слова, которые никогда не разделяются
- Аббревиатуры ("т.е.", "и т.д.", "etc.", "e.g." и т.д.)
- Единицы измерения ("kg", "km", "sec", "min" и т.д.)
- Комбинации аббревиатур с числами ("рис. 1", "стр. 5")

### 3. Составные выражения
- **ТОЛЬКО**: многословные предлоги, географические названия, компании
- **НЕ включает**: пары "subject+verb" типа "i have", "we will" (убраны в v1.2 для устранения конфликтов)
- Фразеологические конструкции ("так как", "потому что", "in order to", "according to" и т.д.)
- Географические названия:
  - Города: "New York", "Los Angeles", "Нижний Новгород", "Великий Новгород" и т.д.
  - Регионы: "New South Wales", "British Columbia", "Республика Саха" и т.д.
- Компании и бренды:
  - "General Motors", "Goldman Sachs", "Альфа Банк", "Яндекс Такси" и т.д.

### 4. Специальные символы
- Открывающие символы: `"`, `'`, `(`, `[`, `{`, `«`
- Специальные символы: `,`, `.`, `!`, `?`, `:`, `;`

## 🔧 Технические детали

### Поддерживаемые типы переносов строк
- `\r\n` (CRLF - Windows)
- `\r` (CR - старые Mac)
- `\n` (LF - Unix/Linux)

### Приоритеты разделения
1. **Переносы строк** (только для "Split into Layers")
2. **Символ разделения** (если включен чекбокс "Use Split Symbol")
3. **Автоматический поиск** - ближайший пробел к середине текста с учетом правил

### Порядок применения правил форматирования (при автоматическом поиске)
1. **Составные выражения** (многословные предлоги, географические названия, компании) - приоритет #1
2. **Слова из avoidEndWords** (предлоги, артикли, союзы, местоимения) - приоритет #2
3. **Слова, которые никогда не разделяются** (аббревиатуры, единицы измерения) - приоритет #3
4. **Специальные термины** (города, бренды) - приоритет #4
5. **Базовые правила** (открывающие символы, специальные символы) - приоритет #5

### Множественный выбор слоев
- При выборе нескольких слоев для "Split into Layers" - разделение происходит по середине времени каждого слоя
- При выборе нескольких слоев для "Split into Lines" - каждый слой обрабатывается отдельно
- При переименовании - все выбранные слои переименовываются и сортируются по `inPoint`

### Переименование слоев
- Если в маске есть `{num}` - заменяется на 4-значный номер
- Если `{num}` нет - номер добавляется в конец
- **Порядок нумерации зависит от чекбокса "Up"**:
  - **"Up" выключен**: Слои нумеруются от самого раннего к самому позднему inPoint (0001, 0002, 0003...)
  - **"Up" включен**: Слои нумеруются от самого позднего к самому раннему inPoint (самый большой номер для самого раннего inPoint)
- Примеры (с "Up" выключен):
  - Маска: `"Text {num}"` → `"Text 0001"`, `"Text 0002"`
  - Маска: `"Scene"` → `"Scene 0001"`, `"Scene 0002"`

## ⚠️ Ограничения

- Скрипт работает только с текстовыми слоями для разделения
- Для переименования поддерживаются все типы слоев
- Требуется активная композиция
- Требуется хотя бы один выбранный слой

## 🐛 Известные проблемы

- При множественном выборе для "Split into Layers" всегда используется середина времени слоя (не учитывается позиция курсора)
- Рекурсивное разделение строк может занять время при большом количестве строк

## 📚 Структура кода

Подробная карта всех функций и структур данных находится в файле [SCRIPT_MAP.md](SCRIPT_MAP.md).

### Основные компоненты:
1. **UI инициализация** (строки 1-85)
2. **Массивы данных** (строки 91-243)
3. **Функции валидации** (строки 248-283, 1002-1047)
4. **Функции разделения** (строки 288-621)
5. **Функции правил форматирования** (строки 626-978)
6. **Функции переименования** (строки 1052-1177)
7. **Обработчики событий** (строки 1182-1195)

### Дополнительная документация:
- **[SCRIPT_MAP.md](SCRIPT_MAP.md)** - Карта всех функций с номерами строк
- **[CODE_AUDIT.md](CODE_AUDIT.md)** - Детальный аудит кода, проблемы и рекомендации

## 🔄 История версий

### v1.2
- **Критическое исправление**: Очищены списки `avoidEndWords` и `compoundExpressions` для устранения конфликтов
  - Убраны вспомогательные глаголы, наречия, количественные слова из `avoidEndWords`
  - Убраны пары "subject+verb" из `compoundExpressions`
  - Теперь "I have a dream" корректно разделяется на "I have" + "a dream"
- Упрощена логика проверки составных выражений
- Улучшена функция `findCompoundSafePosition` для более точного поиска позиции
- Исправлена проверка `lineSplitInfo.position` в `updateLayerText`
- **Добавлен чекбокс "Up"** для инвертированного порядка нумерации и сортировки
  - Когда включен: самый ранний inPoint получает самый большой номер и размещается внизу
  - Исправлена нумерация для соответствия хронологическому порядку inPoint
- Убраны отладочные сообщения
- Обновлена документация

### v1.1
- Добавлена поддержка множественного выбора слоев
- Добавлена функция переименования и нумерации слоев
- Расширены правила форматирования (географические названия, компании)
- Улучшена обработка переносов строк
- Добавлена рекурсивная обработка множественных строк

### v1.0
- Первоначальная версия
- Базовое разделение текста
- Базовые правила форматирования

## 📞 Контакты

**Email**: tannenspiel@gmail.com

## 📄 Лицензия

Скрипт предоставляется "как есть" без каких-либо гарантий.

